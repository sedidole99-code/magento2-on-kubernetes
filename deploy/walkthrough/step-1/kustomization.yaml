apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: step-1

resources:
- ../../bases/app
- ../../bases/database
# Search backend. Exactly one of the two lines below must be active.
# See README "Search engine: OpenSearch / Elasticsearch toggle" for the full procedure.
- ../../bases/opensearch
# - ../../bases/elasticsearch
