apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: step-4

resources:
- ../step-3
- ../../bases/backup
- ../../bases/rabbitmq

patchesStrategicMerge:
- patches/rabbitmq.yaml

configMapGenerator:
- name: additional
  behavior: merge
  envs:
  - config/additional.env
