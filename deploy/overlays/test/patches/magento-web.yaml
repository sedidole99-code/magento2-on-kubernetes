apiVersion: apps/v1
kind: Deployment
metadata:
  name: magento-web
spec:
  template:
    spec:
      initContainers:
      - name: setup
        image: mymagento/magento2:latest
        command:
        - /bin/bash
        args:
        - -c
        - |
          php bin/magento setup:db:status
          php bin/magento app:config:status
