server {
    listen 8080;
    root /usr/share/nginx/html;
    index index.html;

    auth_basic "m2kube Services";
    auth_basic_user_file /etc/nginx/auth/.htpasswd;

    location /data/ {
        alias /usr/share/nginx/html/data/;
    }
}
